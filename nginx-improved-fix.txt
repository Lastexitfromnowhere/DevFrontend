server {
    listen 80;
    server_name lastexitvpn.duckdns.org;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name lastexitvpn.duckdns.org;

    ssl_certificate /etc/letsencrypt/live/lastexitvpn.duckdns.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/lastexitvpn.duckdns.org/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    # Augmenter les timeout pour tous les emplacements
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;

    # Nouvelle section pour gérer les requêtes directes à /dht/
    location /dht/ {
        # Retirer le préfixe /dht/ lors du transfert
        proxy_pass http://127.0.0.1:10001/dht/;  # Notez le slash à la fin
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Supprimer les en-têtes CORS existants pour éviter la duplication
        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Methods;
        proxy_hide_header Access-Control-Allow-Headers;
        proxy_hide_header Access-Control-Allow-Credentials;

        # Ajouter les en-têtes CORS (seulement pour les requêtes non-OPTIONS)
        if ($request_method != 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' 'https://wind-frontend-rosy.vercel.app, https://wind-frontend-last-exits-projects.vercel.app' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, x-wallet-address' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
        }

        # Gestion spécifique pour les requêtes OPTIONS
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' 'https://wind-frontend-rosy.vercel.app, https://wind-frontend-last-exits-projects.vercel.app' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, x-wallet-address' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Content-Type' 'text/plain charset=UTF-8';
            add_header 'Content-Length' 0;
            return 204;
        }
    }

    # Rediriger les requêtes DHT via API vers le serveur DHT
    location /api/dht/ {
        proxy_pass http://127.0.0.1:10001/dht/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Supprimer les en-têtes CORS existants pour éviter la duplication
        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Methods;
        proxy_hide_header Access-Control-Allow-Headers;
        proxy_hide_header Access-Control-Allow-Credentials;

        # Ajouter les en-têtes CORS (seulement pour les requêtes non-OPTIONS)
        if ($request_method != 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' 'https://wind-frontend-rosy.vercel.app, https://wind-frontend-last-exits-projects.vercel.app' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, x-wallet-address' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
        }

        # Gestion spécifique pour les requêtes OPTIONS
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' 'https://wind-frontend-rosy.vercel.app, https://wind-frontend-last-exits-projects.vercel.app' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, x-wallet-address' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Content-Type' 'text/plain charset=UTF-8';
            add_header 'Content-Length' 0;
            return 204;
        }
    }

    # Rediriger les requêtes API vers le serveur principal
    location /api/ {
        proxy_pass http://127.0.0.1:10000/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Supprimer les en-têtes CORS existants pour éviter la duplication
        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Methods;
        proxy_hide_header Access-Control-Allow-Headers;
        proxy_hide_header Access-Control-Allow-Credentials;

        # Ajouter les en-têtes CORS (seulement pour les requêtes non-OPTIONS)
        if ($request_method != 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' 'https://wind-frontend-rosy.vercel.app, https://wind-frontend-last-exits-projects.vercel.app' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, x-wallet-address' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
        }

        # Gestion spécifique pour les requêtes OPTIONS
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' 'https://wind-frontend-rosy.vercel.app, https://wind-frontend-last-exits-projects.vercel.app' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, x-wallet-address' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Content-Type' 'text/plain charset=UTF-8';
            add_header 'Content-Length' 0;
            return 204;
        }
    }

    # Gérer les requêtes auth spécifiquement
    location /auth/ {
        proxy_pass http://127.0.0.1:10000/auth/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Supprimer les en-têtes CORS existants pour éviter la duplication
        proxy_hide_header Access-Control-Allow-Origin;
        proxy_hide_header Access-Control-Allow-Methods;
        proxy_hide_header Access-Control-Allow-Headers;
        proxy_hide_header Access-Control-Allow-Credentials;

        # Ajouter les en-têtes CORS (seulement pour les requêtes non-OPTIONS)
        if ($request_method != 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' 'https://wind-frontend-rosy.vercel.app, https://wind-frontend-last-exits-projects.vercel.app' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, x-wallet-address' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
        }

        # Gestion spécifique pour les requêtes OPTIONS
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' 'https://wind-frontend-rosy.vercel.app, https://wind-frontend-last-exits-projects.vercel.app' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization, x-wallet-address' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Content-Type' 'text/plain charset=UTF-8';
            add_header 'Content-Length' 0;
            return 204;
        }
    }

    # Route pour /status - rediriger vers /dht/status
    location = /status {
        return 301 https://$host/dht/status;
    }

    # Servir l'application Node.js pour toutes les autres routes
    location / {
        proxy_pass http://127.0.0.1:10000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
